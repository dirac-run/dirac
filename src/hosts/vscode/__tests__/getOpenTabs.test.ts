import { describe, it } from "mocha"

// Skipped until gRPC host bridge is fixed in the unit test environment.
describe("getOpenTabs", () => {
	it.skip("should return open tabs via gRPC", () => {})
})
