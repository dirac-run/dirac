import { describe, it } from "mocha"

// Placeholder suite — requires ripgrep binary which is not available in the unit test environment.
// Convert to real tests when ripgrep is bundled or mocked.
describe("Ripgrep regexSearchFiles", () => {
	describe("regexSearchFiles", () => {
		it.skip("should search for a regex pattern in a directory", () => {})
		it.skip("should respect the filePattern filter", () => {})
		it.skip("should respect the excludeFilePatterns filter", () => {})
		it.skip("should return results with line numbers and content", () => {})
	})
})
